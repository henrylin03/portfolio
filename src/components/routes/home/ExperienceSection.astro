---
import type { Workplace } from "@/data/workplaces";
import { Image } from "astro:assets";

interface Props {
  workplaces: Workplace[];
}

const { workplaces } = Astro.props;
---

<section class="max-w-6xl px-20 py-50">
  <h2 class="my-12 text-8xl font-light">Experience</h2>
  <ul class="grid grid-cols-3 gap-10 divide-x-2 divide-gray-200">
    {
      workplaces.map((w) => (
        <li class="flex gap-4">
          <figure class="sketched grid h-20 w-20 place-items-center rounded-2xl border-2 border-gray-500">
            <Image
              src={w.img}
              alt={w.employer}
              loading="lazy"
              class="h-14 w-14"
            />
          </figure>
          <div class="flex flex-col">
            <h3 class="font-semibold">{w.employer}</h3>
            <p>{w.position}</p>
            <p class="mt-1 text-sm text-gray-400">
              {w.startMonthYear} - {w.endMonthYear}
            </p>
          </div>
        </li>
      ))
    }
  </ul>
</section>

<style>
  .sketched {
    border-top-left-radius: 255px 15px;
    border-top-right-radius: 15px 225px;
    border-bottom-right-radius: 225px 15px;
    border-bottom-left-radius: 15px 255px;
  }
</style>
